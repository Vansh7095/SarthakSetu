# SarthakSetu — Development Environment Variables
#
# Copy this file to .env and fill in your values.
# Never commit .env files to version control.

# ---------------------------------------------------------------------------
# Node Environment
# ---------------------------------------------------------------------------
NODE_ENV=development

# ---------------------------------------------------------------------------
# Ports
# ---------------------------------------------------------------------------
# Backend API port (default: 8080)
PORT=8080

# Frontend Vite dev server port (default: 5173)
FRONTEND_PORT=5173

# ---------------------------------------------------------------------------
# Base Path
# ---------------------------------------------------------------------------
# The base URL path for the frontend SPA. Use "/" for local dev.
BASE_PATH=/

# ---------------------------------------------------------------------------
# API access control
# ---------------------------------------------------------------------------
# Leave empty for local/Replit development. Production should set this to the
# public HTTPS origin (or a comma-separated list of allowed browser origins).
CORS_ORIGIN=

# ---------------------------------------------------------------------------
# PostgreSQL
# ---------------------------------------------------------------------------
# Used by Docker Compose to generate DATABASE_URL automatically.
# For local development, fill in concrete values below.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=sarthaksetu
POSTGRES_PASSWORD=changeme
POSTGRES_DB=sarthaksetu

# Local development connection string.
# For Docker Compose, this value is generated from the POSTGRES_* variables above.
DATABASE_URL=postgres://sarthaksetu:changeme@localhost:5432/sarthaksetu

# ---------------------------------------------------------------------------
# Clerk Authentication
# ---------------------------------------------------------------------------
# Get these from https://dashboard.clerk.com after creating an application.
# Use "Development" instance keys for local development.
#
# Frontend publishable key (starts with pk_test_ or pk_live_)
CLERK_PUBLISHABLE_KEY=pk_test_your_key_here

# Backend secret key (starts with sk_test_ or sk_live_)
CLERK_SECRET_KEY=sk_test_your_key_here

# Vite-specific frontend key (same as CLERK_PUBLISHABLE_KEY)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_key_here

# Optional: Clerk proxy URL for custom domains.
# Leave empty for local development.
VITE_CLERK_PROXY_URL=

# ---------------------------------------------------------------------------
# Logging
# ---------------------------------------------------------------------------
# Log level: trace, debug, info, warn, error, fatal
LOG_LEVEL=info
